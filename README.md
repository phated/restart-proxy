# Proxy for my web server

Proxy server that restarts when the there is a change to the routes file.

## Usage

Run `npm install` to install dependencies

Run `npm start` (with superuser permissions) to start the server